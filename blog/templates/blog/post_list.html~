{% extends 'blog/base.html' %}
{% block content %}
 {% for post in posts %}
		<div class="post">
		    <div class="date">

		    </div>
		    <h1><a href="{% url 'post_detail' pk=post.pk %}">{{ post.title }}</a></h1>
		    <b> <p>{{ post.text|linebreaks }}</p></b>
		    <b><i><p style="text-align:right" >published: {{ post.publish_date }}</p>
		      <hr>
		</div>
	    {% endfor %}
{% endblock content%}
